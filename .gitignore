# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files (NUNCA commitar segredos — NEXTAUTH_SECRET etc.)
.env
.env.production
.env*.local
.env.development.local
.env.test.local
.env.production.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# prisma / banco de dados (NUNCA commitar — contém CPF/RG/dados de clientes)
/prisma/*.db
*.db
*.db-journal
*.sqlite
dev.db

# Backups do banco (NUNCA commitar — contém CPF/RG/dados de clientes)
/backups

# Modelos/extrações de contratos (podem conter dados reais de clientes)
/contracts/*.pdf
/contracts/*.docx
/contracts/*-extraido.txt
/public/uploads
